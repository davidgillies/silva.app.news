This macro is provided so the feed footer can be customized. The
idea is to keep the 'inv' template generic (but you'll probably 
hack into that one too ;)

<metal:block define-macro="footer">
  <p class="footer" 
      tal:condition="python:options['show_footer']=='1'">
    <a tal:attributes="href python:viewerobj.absolute_url() + '/rss'"
        title="Get the source....">
      <img tal:replace="structure here/rss10.gif | nothing" />
    </a>
  </p>
</metal:block>
